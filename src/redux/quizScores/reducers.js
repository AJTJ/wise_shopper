// import * as constants from "./constants";

const defaultState = {
  wise_score: { yes: 0, sometimes: 0, no: 0 }
};

const scoreReducer = (state = defaultState, action) => {
  return state;
};

export default scoreReducer;
